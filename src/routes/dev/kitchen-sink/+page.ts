// Internal design-system reference. Indexed = false (see robots.txt and noindex meta).
// CSR is required here so the ThemeToggle component can hydrate.
export const csr = true;
export const prerender = true;
